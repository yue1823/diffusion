export declare const isValidTransaction: (transaction: Record<string, any>) => {
    isValid: boolean;
    invalidMsg?: string;
};
export declare const isValidTransactions: (transactions: Record<string, any>[]) => {
    isValid: boolean;
    invalidMsg?: string;
};
