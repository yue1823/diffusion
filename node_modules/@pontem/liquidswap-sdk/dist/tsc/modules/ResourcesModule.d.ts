import { SDK } from '../sdk';
import { IModule } from '../interfaces/IModule';
import { AptosResource, AptosResourceType } from '../types/aptos';
export declare class ResourcesModule implements IModule {
    protected _sdk: SDK;
    get sdk(): SDK;
    constructor(sdk: SDK);
    fetchAccountResource<T = unknown>(accountAddress: string, resourceType: AptosResourceType): Promise<AptosResource<T> | undefined>;
}
