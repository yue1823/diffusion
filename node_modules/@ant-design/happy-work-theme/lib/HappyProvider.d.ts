import * as React from 'react';
export interface HappyProviderProps {
    disabled?: boolean;
    children?: React.ReactNode;
}
export default function HappyProvider(props: HappyProviderProps): React.JSX.Element;
